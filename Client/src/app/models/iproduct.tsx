export interface iproduct {
  id: number;
  code: string;
  name: string;
  quantity: number;
  price: number;
  description: string;
  categoryId: number;
  subCategoryId: number;
  pictureUrl: string;
}
